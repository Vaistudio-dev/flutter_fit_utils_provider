name: flutter_fit_utils_provider
description: "A flutter package to easily create providers of modelable data. This package is part of the flutter_fit_utils environement."
version: 1.0.2
homepage: https://pub.dev/packages/flutter_fit_utils_provider
repository: https://github.com/s0punk/flutter_fit_utils_provider
issue_tracker: https://github.com/s0punk/flutter_fit_utils_provider/issues

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - provider

environment:
  sdk: '>=3.3.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_fit_utils: ^2.2.0
  provider: ^6.1.2
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: